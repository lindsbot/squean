'use strict';

var Sequelize = require('sequelize');

module.exports = function(app) {
  // var db = new Sequelize(app.get('db'), 'root', 'plantlife');

  // var models = [
  //   'users'
  // ];

  // models.forEach(function(model){
  //   module.exports[model] = db.import(__dirname + '/' + model);
  // });
};